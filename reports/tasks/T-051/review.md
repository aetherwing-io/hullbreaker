REQUEST_CHANGES

- SPRINT.md:1213 (T-051 accept box: "far edge dissolves into the fog color, proven by capture") — unmet, and disproven by the lane's own committed evidence. `reports/tasks/T-051/evidence/02-facet1-plates-after.png` (facet 1, scrollX=56m, the `limbSegment`/near-tier plate placed at `src/config.js:1111` — `depth: -13`) shows a hard, straight rectangular boundary where the plate meets the flat sky color, not a fog dissolve. Independently reproduced with a fresh capture at scrollX≈18m (1280x720): the seam sits at the predicted screen position and is pixel-sharp. Root cause, also independently verified: `assets/generated/backdrops/backdrop-limb-segment.png`'s alpha channel is ~99.5% pure 0/255 (only 0.48% of pixels are partial/antialiased edge pixels — measured via PIL), i.e. an effectively hard-edged cutout, not a feathered silhouette; and because the quad is a single flat, camera-facing plane, `fog: true` at `src/render/backdrop.js:121` tints the whole face roughly uniformly rather than softening its edge — no depth/fog tuning on a flat plane can make a hard alpha cutout "dissolve" at its boundary. `tools/pathcheck/t-051-backdrop.mjs`'s fog-ladder check (the "graded, not flat, not fully clamped" section) only asserts each tier's haze fraction is `>0 and <1`, which does not bind this requirement. Scope: this is specific to the `near` tier / `limbSegment` plate (used at faces 1/3/5, 3 of 12 placements) — the `mid`/`far` tiers in the lane's own `04-facet2-plates-after.png` are dark/hazy enough that the same hard-alpha edge does not read as a seam there. The build report (`reports/tasks/T-051/build.md:221-228`) does not claim this box is satisfied; it reframes it as an open question for the operator ("does it read as ... a flat image pasted over the sky?"), which is not the same as proving a written, capture-testable acceptance criterion (decisions.md entry 10). Fix direction is a depth/tint retune of the `near` tier (and re-proof by capture), not an operator feel call — the box as written names a falsifiable test and that test currently fails.

Everything else checked out: pathcheck count (2748→3024, +276) reproduced exactly against `git show 32ca489`'s own parent in an isolated scratch copy, and is fully accounted for by the file's own loops (no inflation) — 5 of the new assertions independently broken/restored (play-band clearance, canvas-dims-match-file, scene.js-must-not-import-backdrop.js, sim/pure ban, no-second-TextureLoader), all bound correctly. The `scene.js`→`backdrop.js` circular-dependency claim justifying the `src/main.js` fence departure is real: reproduced independently with a minimal Node ESM repro (static import → caught `Cannot access 'renderer' before initialization`, TDZ, silently inert forever; awaited dynamic import → genuine hang, confirmed with a bounded timeout). `src/render/backdrop.js` is genuinely reached from `src/main.js`. Entry-16 degrade-safety independently verified by intercepting network requests (404 one plate, 4s-delay another past the 2500ms budget): boot still reached `PLAYING`, no page errors, 7/12 slots built, the rest cleanly `"failed"` with an honest reason. `?backdrop=flat` A/B, sim/pure layers not referencing backdrop state (broken/restored), static anatomy and the FAR camera (`scene.js`/`camera.js` carry zero diff in this commit) all hold. `src/config.js`/`src/render/palette.js` edits are additive/tokens-only, no frozen `CONFIG` movement constant moved. Perf reproduced independently in a clean scratch copy: worstMs/over20ms unchanged across a 3-run distribution, drawCalls 150→174 (+24, one per quad), matching the report.
