APPROVE

src/mode.js:99 — MINOR: any unrecognized `?polyp=` value (e.g. `?polyp=3`) resolves to the solo stage rather than off. Deliberately unchanged in the fix cycle; it mirrors the shipped `?hound=` convention exactly, absent-param default stays off, and pathcheck asserts every no-flag pace x hound-stage plan is byte-identical. Symmetry note only.

Second-pass verification of the prior findings: (1) the MAJOR (missing operator feel questions) is fixed — reports/tasks/T-004/evidence/README.md now ends with "Open feel questions for the operator (we do not judge fun)": exact serve URLs for ?polyp=1 and ?polyp=2 plus five questions (tell/vent readability, lane-lock vs stat wall per entry 6, drop-through discoverability, combo double-bind readability, barrel silhouette at FAR scale), ready to lift into SPRINT's checkpoint queue. (2) The open-loop-approach MINOR is fixed — all four scripts are fully closed-loop (zero timed movement inputs; the only scheduled input left is vent-kill's fire hold, state-independent by design since spraying through armour states is the test), and the regenerated evidence traces match the summaries: lane-dodge completed with fire onsets at 3919/7669 while the bot stood at y=3.0 below the band (third volley at 11428 with the bot at x≈69.6 on egress); combo-stack completed with both threats fully engaged (polyp closed/tell/fire/vent, hound prowl/tell/charge/skid) and both hits at the 3927/7677 fire onsets as declared; facetank stalls by design with three volleys; vent-kill's polyp hp holds 6 through closed/tell, first drops in `fire` at 3968, and dies mid-`vent` at 4619.

Re-verified clean on the fresh pass: pathcheck green in the worktree (681 passed, 0 failed; the polyp block is purely additive — no weakened or deleted assertions); layer purity holds (src/pure/polyp.js imports nothing; sim/hostiles.js's `IS_TRANSFORM_SLICE`/BENDS import mirrors sim/weapons.js's pre-existing line verbatim; sim→render stays on the existing bridge hostiles hooks, no new crossings); determinism holds (no Math.random/Date.now/performance.now in pure/sim, rhythm on gameMs, 2D (s,y) preserved); materialize rule respected (updatePolyp inert before enterUntil, beam render gated on the same); placement-over-stats honored (hp 6 dies inside one asserted vent window, all damage routed through hitHostile's iris armour, threat is the rooted sightline with asserted reroutes above/below); entry-7 bend rule extended to the beam; frozen movement constants untouched; ?hook=1 untouched; flags default off with byte-identical no-flag plans asserted; combo stage is exactly two enemies and its hound row is the judged rejoin beat's station/span/owned connector; branch-side commits are T-004-only; no per-frame allocations (reused POLYP_POSE, persistent beam mesh, disposed on removal); no new runtime deps, no build step, no OSTK artifacts; docs updated truthfully and the worktree is clean of untracked files.
