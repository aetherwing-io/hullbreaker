REQUEST_CHANGES

src/ui/shell.js:72 — MAJOR: `--rot` is a CSS custom property, so it INHERITS to children; an `attach: true` element therefore re-applies its parent's rotation on top of the parent's own transform (index.html:64 `transform: rotate(var(--rot, 0deg))`). On the shipped default composition ("wake", src/pure/shell.js:143) RIG is attached to the 37° plate and renders at 74° — measured in headless Chrome, the rig's own computed transform is `matrix(0.798636, 0.601815, …)`, identical to its parent's. The figure lies nearly horizontal above the plate instead of running along it, and its rendered vertical extent is 3.45% of frame height, not the 3.8% the data declares. The committed evidence shows it (artifacts/shell-v1/title-wake.png, same frame my own capture reproduced). `climb`/`crown` escape only because their gantry parents carry no `rot`. Fix by zeroing `--rot` on attached children (the same leak applies to `--o`, `--c`, `--w`, `--h`: an attached child silently inherits any of them the parent set). This is the exact screen the operator packet asks for a canon verdict on, so it should be right before the checkpoint goes out.

docs/DESIGN.md:479 — MINOR: "The compositions obey the same invariants the game does: RIG at 3–5% of frame height (board 13) … so `tools/pathcheck.mjs` asserts them headlessly" (and artifacts/shell-v1/README.md:45 "Board 13's scale rule holds on the title too") claims more than tools/pathcheck.mjs:4484 checks — the assertion reads the composition DATA (`e.h`), never the rendered figure, which is how the 74°/3.45% render above passed a green gate. Either narrow the claim or add a render-side check to the browser self-test.

index.html:107 — MINOR: `.sl-rig::after` (the muzzle flash) is positioned at `left: 108%`, outside the element's own `clip-path` polygon, and clip-path clips pseudo-elements — I reproduced the identical construction in isolation and nothing renders. So `@keyframes sl-flash` (index.html:111) is dead CSS and the "RIG running the plate, firing" / "warm rim and a muzzle flash" descriptions (src/pure/shell.js:141, index.html:105, artifacts/shell-v1/README.md) describe something that never appears on screen.

src/main.js:132 — MINOR: in the traversal slice, `R` pressed at the title resolves to intent `start` (→ PLAYING, deliberately not consumed) and then falls into the pre-existing `KeyR` branch, whose `IS_TRAVERSAL_SLICE` arm calls `resetGame()` — one press both starts and restarts the run, spending an extra attempt (verified: `sliceStats.attempts` 1 → 2 on a single R at the title). Harmless to the world state, but it double-counts the attempt the comment at src/main.js:168 says leaving the title must not spend.

tools/playtest/README.md:35 — MINOR: `--no-testapi` ("test what a debug-flag-free session sees") no longer boots straight into PLAYING — SHELL_AUTOSTART keys off `testapi`/`selftest` only (src/mode.js:128), so that documented fallback now starts frozen at the title until the script's first key. The default path is unaffected (run.mjs appends `testapi=1`), and every other harness entry point — tools/movement/selftest.mjs, viewscale-capture, transform-capture, g1-capture, g1-bendshot — appends `selftest=1`/`testapi=1`, so the contract holds there; the harness README should still note the one exception.
