APPROVE

Second pass. All five findings from the first review are fixed, and I re-verified each in headless Chromium against the worktree rather than taking the commit message's word for it:
- MAJOR (inherited `--rot` re-rotating the attached RIG): fixed at the root. `elementVars()` (src/pure/shell.js:270) resolves the whole property set with defaults and src/ui/shell.js:71 writes all of it on every element. Measured on all three directions: RIG's own computed transform is `matrix(1, 0, 0, 1, 0, 0)`, its parent plate still carries `matrix(0.798636, 0.601815, …)`, and zero elements are missing any of the seven vars. Rendered box 4.54% (wake) / 3.90% (climb) / 3.80% (crown) of frame height — the numbers the commit and artifacts/shell-v1/README.md claim, and inside board 13's 3–5% band.
- muzzle flash: the clip-path moved to `::before`, and the flash now actually paints — sampling the frame over the 1.7s cycle, the screenshot of the RIG's box changes size (8559 → 9202 bytes) exactly on the lit frame.
- R at the title: `sliceStats.attempts` stays 1 through the press that leaves the title (was 1 → 2), while a second R mid-run still restarts (→ 2), so the traversal-slice restart contract is intact. ArrowRight/Space behave the same and ArrowRight still moves the player on the same press, so nothing is swallowed.
- docs: DESIGN.md:479 and artifacts/shell-v1/README.md:50 now scope the headless claim to the composition data and name the render-side self-test; tools/playtest/README.md:43 documents the `--no-testapi` change with the `shell=0` remedy.

Gates I ran myself: `node tools/pathcheck.mjs` in the worktree → 993 passed, 0 failed (961 on main's tip, 980 at the branch's previous commit; +13 new assertions, and `git diff main...HEAD` shows 0 deleted lines in pathcheck.mjs, so nothing was weakened to get green). `?selftest=1` PASS at six-face (23), `&shell=title` (23), traversal at 480×780 (28), `&shell=0` (15), transform (26). Smoke: `scripts/mid-route.json --deterministic` against the worktree completed at testapi fidelity; a shell-on vs `shell=0` pair differs only within the harness's own run-to-run noise (a control pair of identical shell-on runs varies by the same amount), so the shell is inert for the bot harness.

Layer purity, determinism, and verdict compliance all clean: no sim file changed (MENU was already in src/sim/state.js's documented machine), src/pure/shell.js touches no DOM/THREE/`window` and no clock or RNG, CONFIG is untouched, `?hook=1` is untouched, the FAR default is unchanged (the options panel only offers reload links), and the title obeys the static-anatomy rule — the plate is pre-hinged and only light, vapour and haze animate, with a `prefers-reduced-motion` opt-out.

Minor findings below; none blocks the merge.

src/main.js:661 — MINOR: the new render-side scale check measures `rig.getBoundingClientRect().height` (src/main.js:652), which is the axis-aligned bounding box of a rotated element, not the figure's own extent — that is why the wake figure reads 4.54% for a 3.8vh silhouette. It is a looser guarantee than its name suggests: under the bug this cycle fixed (74° total), the same expression yields 2.5·sin74 + 3.8·cos74 = 3.45%, still inside the 3–5% band, so this check alone would not have caught the regression it is presented as guarding. Its two siblings (the inherited-var scan and the identity-transform check) are the ones with teeth and both do catch it, so the guard as a whole is sound — worth either measuring along the figure's own axis or softening the check's name.

src/pure/shell.js:109 — MINOR: the climb direction declares RIG at `h: 3.9`, but src/ui/shell.js:21 ("RIG is 3.8% of frame height on all three directions"), index.html:101 and artifacts/shell-v1/README.md:50 ("declared at 3.8% … in all three directions") all say 3.8. Both values sit inside board 13's 3–5% band so no invariant is broken; the prose is just off by one direction. artifacts/shell-v1/README.md:57 already reports the 3.90% render, which is where the drift shows.

src/main.js:433 — MINOR (perf, small): the frame loop skips `update(dt)` at MENU but still calls `renderer.render(scene, camera)` and `updateHUD()` every frame, and `#shell` (index.html:56) is an opaque `#07131a` fixed layer at z-index 30 covering the viewport — so while parked at the title the WebGL scene is drawn in full and never seen. PAUSED renders for a reason (its overlay is translucent); MENU does not. Gating the render on `state !== 'MENU'` would cost nothing.

src/main.js — MINOR (integration, not a defect in this diff): the branch is behind `main` and no longer merges cleanly. `git merge-tree --write-tree main HEAD` reports one content conflict, in src/main.js — main has since moved the import block (`IS_G2`, `buildTransformPath`/`transformAltAt`), the `sliceStats` reset in `resetGame`, and the transform-slice self-test block, all of which this branch also edits. Everything else (src/mode.js, tools/pathcheck.mjs, tools/playtest/README.md) auto-merges. The integrator will need the same hand merge the repo has precedent for (`Merge main into task/T-006`, `…/T-017`), and should re-run pathcheck plus `?selftest=1` after it, since the merged self-test file will interleave two new blocks.

Note, not a finding: several of the new pathcheck guards assert source text with exact-whitespace regexes (e.g. tools/pathcheck.mjs:4740 and :4745 matching the `startRun(); startedFromTitle = true;` and `!startedFromTitle` lines in src/main.js). They fail closed — a harmless reformat produces a false failure, never a false pass — and the browser self-test covers the same properties behaviorally, so this is acceptable as-is; just expect them to need touching whenever that handler is reformatted.

Out of scope for this gate, for the operator packet: which board-05 direction is canon is unjudged by design, and artifacts/shell-v1/README.md carries the question plus the three URLs. Worth adding to the checkpoint questions now that the figure renders correctly: on the wake composition RIG leans with the 37° plate (its "up" is the plate's normal), which is the correct reading of "standing on the surface" but is a look the operator has not seen judged.
