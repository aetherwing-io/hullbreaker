REQUEST_CHANGES

tools/playtest/README.md:828 — MAJOR. The script-catalogue row for `six-face-aimed-run.json` still reads "it clears wave gates **1, 2 and 3** ... reaching scroll **205 of 415** with **22 kills**". The same file, 470 lines earlier at :352, now says "The ceiling is wave gate 2 / scroll 140 of 415, at about 50 s ... the same ceiling for every variant tried (the shipped aimed policy, ...)", and the evidence this diff commits (tools/playtest/reports/t019/all-runs.md, 8 aimed-run rows) is scroll 140 / gates "1,2" eight times out of eight. The README contradicts itself inside one diff, and the catalogue is the table an agent reads when choosing a script. Update the row (or point it at docs/playtests/2026-08-victory-box.md) so the two halves agree.

tools/playtest/scripts/six-face-aimed-run.json:3 — MAJOR. Same stale claim in the shipped script's own description ("clears wave gates 1, 2 and 3, reaching maxX 219.3 / scroll 205 of 415 with 22 kills before spending its third life at 76.9 s"), and this diff changed the file's behavior underneath it: `terrain.gapDist` now reports 0 whenever RIG is over a hole (tools/playtest/lib/sampler.mjs:200), which changes when this script's `!grounded && vy<0 && terrain.gapDist<1.2` air-jump rule fires — previously, over a one-column hole, the scan reported the distance to the *next* hole ahead. So the description's measurement predates both the field semantics and the tree it was taken on (T-018 pinned task/T-009 at :8751; T-019 measured `main`). The finding never reconciles this: §4 (docs/playtests/2026-08-victory-box.md:195) corrects T-018's airborne claim but not its reach claim, while :53 asserts "Nothing ever reached gate 3." Either annotate the script description with the re-measurement, or say in §4 whether T-018's gate-3 run was tree-specific, sampler-specific, or an unreproduced outlier — a 49-run finding that silently disagrees with a shipped file's own advertised result is the failure mode this task exists to prevent.

tools/pathcheck.mjs:7030 — MAJOR. The anti-scripting guard iterates `script.moves` only. `compileScript` (tools/playtest/lib/compile.mjs:86) also accepts raw `"events": [{t, type, code}]`, merges them with the expanded moves, and dispatches them alongside a policy — so a future `six-face-*.json` can carry a full timestamped keypress timeline and still pass this assertion. That makes the guarantee stated in code at :6968 ("held to relative geometry BY ASSERTION, not by review diligence") and in docs/playtests/2026-08-victory-box.md:250 ("A scripted win now fails the gate instead of passing review") wider than what is enforced, on the one delivery box whose integrity the guard exists to protect. Extend the same loop over `script.events` (reject any code other than the fire key, or reject an `events` array outright for six-face scripts).

tools/playtest/README.md:359 — MINOR. The T-019 summary bullets were not folded with commit cad59bc's final counts. README: "roughly 59 gating bodies", "1.0–1.8 kills per hp", "needs about 6.5", "some ray has a target on only 31–36 %", "already on target for 21–31 %". The finding it cites (docs/playtests/2026-08-victory-box.md:76, :79, :82, :144) says 50–55 bodies, 0.6–2.3 (median 1.3), about 6, 25–36 %, 18–31 %. Two numbers for one claim across two files that reference each other.

tools/playtest/scripts/six-face-spaced-run.json:3 — MINOR. Description quotes the baseline as "median 48.6, 4 runs"; the finding's table (docs/playtests/2026-08-victory-box.md:31) has 5 baseline runs, median 47.5. Same drift as above — the last commit updated the doc, not the script text.

tools/playtest/README.md:820 — MINOR. `six-face-spaced-run.json`, the policy this task actually ships and the entry point its reproduce block leads with, appears nowhere in the README's script catalogue; the string "spaced" does not occur in the file at all. The harness DoD asks for the tool's own README to be updated for a new entry point.

tools/playtest/analyze-run.mjs:281 — MINOR. The dive census `continue`s on `!rules`, so a policy-free run prints "- all: none" — indistinguishable from a run that had no dives. The aim-coverage section directly above (:249) prints an explicit n/a for exactly this case; the census is one honesty note short of matching it.

tools/playtest/analyze-run.mjs:319 — MINOR. `rec.killed = !still && rec.minDist < 14` scores any diver that leaves the roster while it was within 14 tiles as killed, so a culled or despawned hostile counts as a kill. The bias is conservative for the finding's argument (it inflates the "diver killed" column the finding uses to argue kills are too rare), but it is not in the HONESTY NOTES block at :33 the way the "ended in contact" inference is. Also `prevIds` (:279, :323) is assigned and never read.

SPRINT.md:478 — MINOR, likely integrator scope. Accept box 3 asks for the operator packet to be posted; it is written (docs/playtests/2026-08-victory-box.md:253, URL plus five questions) but has no entry in SPRINT's "Operator checkpoint queue", which is the mechanism the loop protocol uses to surface a feel verdict without blocking. No branch change needed if the integrator queues it.

checked: `node tools/pathcheck.mjs` in the worktree — 1589 passed, 0 failed (main: 1517); +72 assertions, zero deletions anywhere in the pathcheck diff. `git diff main...HEAD -- src/ index.html` is empty, so no game file, wave gate or movement constant moved; no new runtime deps, no build step, no OSTK artifacts, nothing outside tools/ and docs/playtests/. New pathcheck marks verified against threat.mjs's muzzle offset (1.05) and SLOPE_CAP (9); both six-face policies compile clean against the grammar. The finding does not judge fun and routes the difficulty questions to the operator, per the machine-gates rule.
