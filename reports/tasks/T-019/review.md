APPROVE

Second pass. All three MAJORs from the first review are fixed, verified against the branch as it stands (fix commit `6ad3fc5`), not against its commit message:

- **README catalogue** (`tools/playtest/README.md:840`) no longer claims gates 1–3 as current. Both measurements are carried, each labelled with its tree and run count (T-018: one run on `task/T-009`@8751; T-019: 11 runs on `main`, ten dead in gate 2 at scroll 140, one in gate 1 at 107). Matches `tools/playtest/reports/t019/all-runs.md` row for row.
- **Script description** (`tools/playtest/scripts/six-face-aimed-run.json:3`) re-stated the same way, and the new `docs/playtests/2026-08-victory-box.md:245` §4.2 reconciles the disagreement explicitly: unreproduced over eleven runs, with the tree change *and* this task's own `gapDist` change named as the two uncontrolled differences and neither claimed as the cause.
- **The anti-scripting guard** (`tools/pathcheck.mjs:7044`) now checks the raw `events` door as well as `moves`, and is asserted to have teeth against a synthetic scripted win in all three forms (absolute-position clause, timed `move`, timed raw `event`) plus a no-policy timeline.

The three doc-drift MINORs (README bullets, spaced-run baseline quote, missing catalogue row) and both analyze-run MINORs are also fixed: README's bullets now read 50–55 bodies / 0.6–2.3 kills per hp median 1.3 / needs ~6 / 23–36 % available with the 20-run split spelled out; the spaced script quotes median 48.7 over 8 default-rate runs; `six-face-spaced-run.json` has its own catalogue row; the dive census prints an explicit n/a on a policy-free run (`tools/playtest/analyze-run.mjs:350`); the killed-by-vanishing inference is in the header honesty notes (`:50`) and inline (`:322`); `prevIds` is gone.

Remaining, all MINOR:

tools/pathcheck.mjs:7039 — MINOR. The move check passes a move if *either* `hold` or `tap` names the fire key, but `compileScript` dispatches only `hold` when both are present (`tools/playtest/lib/compile.mjs:47`, `if ('hold' in m) … else if ('tap' in m)`). So `{"hold":"jump","tap":"fire","fromMs":…,"toMs":…}` dispatches a held jump and passes the guard. Contrived shape (compile silently drops the `tap`), and no shipped script does it, but the guarantee stated at `tools/pathcheck.mjs:6968` and in `docs/playtests/2026-08-victory-box.md:15` is "by assertion, not review diligence". One-line fix: test the key compile would actually dispatch — `'hold' in m ? m.hold : m.tap`.

tools/pathcheck.mjs:7009 — MINOR. `RELATIVE_OK` admits `hudTC`, and the policy grammar supports `==`/`!=` against strings (`tools/playtest/lib/policy.mjs:174`), while the HUD's centre line during a gate is `WAVE k/6 — N HOSTILES` (`src/ui/hud.js:141`). So `hudTC=='WAVE 3/6 — 5 HOSTILES'` would pass a guard whose own comment allows HUD strings only when they "name a PHASE … rather than a place". Still closed-loop (no clock, no coordinate), and no script does it; worth either restricting `hudTC` to a truthiness test or softening the comment's claim.

tools/playtest/scripts/six-face-aimed-run.json:3 — MINOR. The rewritten description still says "Same shape as six-face-full-run.json" and compares against "the aimless script"; that file exists in neither `scripts/` nor on `main`. Dangling reference in a line this diff otherwise re-measured.

SPRINT.md:478 — MINOR, integrator scope, unchanged from the first pass. Accept box 3's operator packet is written (`docs/playtests/2026-08-victory-box.md:320` — URL plus five questions) but has no entry in the checkpoint queue, which is how the loop protocol surfaces a feel verdict without blocking. The branch correctly does not touch `SPRINT.md` (lane discipline); the integrator queues it.

checked: `node tools/pathcheck.mjs` inside the worktree — **1529 passed, 0 failed, exit 0** (the branch's merge-base main was 1517; current main is 1527 after other merges). Zero assertions weakened or deleted anywhere in the pathcheck diff; the guard passes on the pre-existing `six-face-aimed-run.json` without its rules being touched (that file's diff is the description line only). `git diff main...HEAD -- src/ index.html assets/` is empty, so no game file, wave gate or movement constant moved — accept box 2 holds, and pathcheck now enforces the harness-side half of it. `tools/playtest/lib/compile.mjs` imports nothing, so pathcheck's new `await import` adds no dependency: verified by running the gate in a worktree with **no `tools/playtest/node_modules`**. No new runtime deps, no build step, no OSTK artifacts, nothing outside `tools/` and `docs/playtests/`; `--stop-on-game-over` defaults off and the driver's outcome computation is unchanged. Determinism/layer purity untouched (harness-only; the sampler runs in-page during playtests and ships nothing). Only the two six-face scripts read `terrain.gapDist`, so the field's semantic change invalidates no other committed demo baseline, and both scripts carry the re-measurement. Evidence spot-checked against the claims: `all-runs.md` gives median 48.7 over the 8 default-rate aimed runs and 53.1 over the 9 spaced runs, exactly as the two script descriptions, the README row and the finding's §1 addendum state; the committed `analysis.txt` files match their quoted 52.0 s / 49.8 s / 64.4 s-at-scroll-165 runs; the committed `expJ-strafe.json` variant is relative-geometry-only, as the finding claims of all its variants. The finding argues from medians and from which gate ended the run, states its inference biases in the tool header (`tools/playtest/analyze-run.mjs:33`), declines to judge difficulty, and routes the five difficulty questions to the operator — machine-gates-never-judge-fun respected.
