APPROVE

- reports/tasks/T-052/evidence/stress/result.json:1 — stale texture-memory snapshot. The committed JSON reports `textures: 32` (textured) vs `25` (flat), which is the PRE-disposal-fix number quoted in build.md §6; the addendum's later fix (disposing the four raw textures once each canvas consumes them) drops this to 29 per the prose. I independently re-measured live against a scratch copy of HEAD (fresh `renderer.info.memory` read via `page.evaluate` importing `/src/render/scene.js`) and got **29 vs 25 (+4)**, matching the addendum's prose, not the checked-in file. Benign direction (the true number is smaller/better than what's on disk) and the prose text is accurate, but the artifact under `evidence/` was not regenerated after the fix it is supposed to be evidence for.

Verification performed (all independent, not inherited):
- Scoped the diff to T-052's own commits (`git diff 5c8008e a9b70a6`, i.e. against the T-049 branch point this lane was dispatched on top of) rather than the two-dot `main..HEAD` diff, which is polluted by unmerged concurrent lanes (T-049/T-051/T-053) per the lane brief's own warning. Authored scope: `src/render/hulltiles.js` (new), `src/render/materials.js` (new), `src/render/limb.js` (+27/-5), `tools/pathcheck/manifest.mjs`, `tools/pathcheck/t-052-hull-texture.mjs` (new), `reports/tasks/T-052/**`.
- pathcheck delta computed myself: base (`5c8008e`, scratch copy) 2748 passed → worktree HEAD 2788 passed, +40, exactly matching build.md's claim. Exit 0 both sides.
- Layer purity: pathcheck's own static `guardLayer()` (tools/pathcheck/_context.mjs) scans all of `src/pure/` and `src/sim/` on every run and passed; independently grepped both dirs for THREE/document/window — only English-word/comment hits (`traversal.js:374` "window", THREE-free comments), no real references. `hulltiles.js` (the one new file reachable from pathcheck) is confirmed Node-safe by its own assertion #1 and by inspection. No `src/pure/`or `src/sim/` file is in T-052's diff at all.
- Determinism: no `Math.random`/`Date.now`/`performance.now` added; `src/config.js` untouched, so no frozen jump/movement constant moved.
- Behavioral proof of a new assertion, done myself in a scratch copy (git archive of HEAD, not the live worktree): commented out `applyHullTexture(material, key);` in `limb.js` → pathcheck dropped to 2787/1 failed with exactly the predicted message (`bakeLimb() calls applyHullTexture(material, key) for every bucket it builds`); restored → back to 2788/0. Matches build.md's own break/restore table for that line.
- Item 1 (normalization is runtime/asset-agnostic): confirmed by reading `normalizeScale()`/`meanLuminance()` in materials.js — the scale is computed from `meanLuminance(img)` on whatever image object actually loaded (24x24 sample canvas), converges iteratively, and `TARGET_MEAN`/`NORMALIZE_SAMPLE_PX` are the only constants, neither of which encodes today's tile's own mean or color. No per-bucket hardcoded brightness number exists anywhere in the new code.
- Item 2 (hue-shift fix): verified by construction, not just by eye — `tileFilter()` applies `grayscale(100%)` before `brightness()`, and CSS canvas filters compose left-to-right, so the map that reaches the GPU has R=G=B at every texel. Multiplying a material's (possibly warm or cool) `color` by a map with equal channels can only ever scale all three channels by the same factor, which preserves hue/saturation exactly and cannot reproduce the reported blue-crush — this holds for any tile color, not just hull-panel-tile.png's. Detail is not destroyed: grayscale removes hue only, leaving the tile's own luminance pattern (panel lines/rivets) intact for both the albedo multiply and the reused bump map, which wants luminance data anyway.
- Item 3 (residual 33%→29% darkening): build.md states this in bold, with numbers, as a linear-space multiply-against-white cost, shows a further attempt to close it (`TARGET_MEAN` 210→255) that only closed 39%→35% before flattening out, and explicitly declines to self-judge it ("that word 'reads' is exactly the kind of judgement that isn't mine to make"). This is a stated, measured tradeoff, not a rationalization dressed as a fix.
- Item 4 (degrade safety, entry 16): reproduced myself in the scratch copy — removed `hull-panel-tile.png`, reloaded with playwright-core; selftest still PASS (39/39), no page errors, `window.__HB_HULL_TEX()` correctly reported `hull`/`wall` absent from `buckets` (the two consumers of that one file) while `scute`/`shadow` (fed by the other two files) stayed present. Restored the file afterward; scratch copy only, live worktree untouched throughout (`git status --short` clean in the worktree at every check).
- Item 5 (perf): draw calls (178) and triangle count (105496 in the committed evidence / 105334 in my lighter non-stress read, difference is just idle-vs-loaded hostile/projectile count, not a regression) are identical textured vs flat — no new meshes, only material properties changed on the same InstancedMesh buckets. `bakeLimb()` (and therefore `applySurface`/`applyHullTexture`) runs exactly once at module scope (`export const limbPieces = IS_G1 ? bakeLimb() : 0;`), not per frame — no hot-loop allocation added. Texture memory re-measured directly (see finding above): +4 net resident textures, not the stale +7 in the evidence JSON.
- Scope/hygiene: no new runtime dependency, no build step, `?tex=flat` is the only opt-out and resolves correctly for every bad input tested in both the assertion suite and by inspection; fenced files (`preload.js`, `backdrop.js`, `scene.js`, `level.js`, `transform.js`, `SPRINT.md`, `CLAUDE.md`, `README.md`, `assets/generated/**`) are absent from the scoped diff, confirmed by `git diff` against each.
