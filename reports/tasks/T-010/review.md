REQUEST_CHANGES

SPRINT.md:327-329 — MAJOR: the operator checkpoint packet still ends "Note: enemy meshes are still grey-box green this pass (acid-green tokens land after the hostiles lane merges)." That is now false. This fix-cycle wired `src/render/hostiles.js` to the palette tokens (LOOK at src/render/hostiles.js:151-164 reads `PAL.wasp/carrier/hound/polyp`, plus tells/beam/vent), removed the pathcheck exemption, and commit 114910e recaptured all pairs *because* the pass-1 frames were stale — so the committed evidence in `artifacts/palette-v1/` shows acid-green hostiles while the packet tells the operator they are grey-box. The packet is the only operator-facing artifact of this task and this is the exact accept item the fix-cycle exists to deliver; a false brief risks the operator skipping the enemy-color judgment (or distrusting the packet against the images). Same fix, second gap: the URL list at SPRINT.md:321 omits the new sixth scene (`?polyp=1`, `artifacts/palette-v1/polyp-trial--*.png`) that 114910e added specifically as the frame where the acid body, warm tell, and hot beam are all readable. Replace the note with "the acid-green hostile ecology (incl. T-004's Iris Polyp) is live on every enemy mesh; tells stay warm amber", add `?polyp=1` to the URL list, and point a question at whether the acid bodies and warm tells separate at FAR against boards 01/06/10. DESIGN.md:11-20 and tools/playtest/README.md were correctly updated in that same commit — only SPRINT was missed.

src/render/palette.js:79,142 — MINOR: `enemyGlow` is authored in both tables and hue-asserted (tools/pathcheck.mjs:4429) but no shipped module reads it (`grep -rn enemyGlow src/` returns only palette.js). A guard certifying a color role that never reaches a mesh reads as coverage it does not have. Wire it or drop it with its assertion.

tools/pathcheck.mjs:4491 — MINOR: the "no raw hex literals in tokenized render files" guard matches only `/0x[0-9a-fA-F]{6}\b/`. CSS-string colors (`'#rrggbb'`, `rgba(...)`) pass through untouched, which is the exact shape of two tokens the palette already carries (`capsuleInk '#14181e'`, `tints`). A future merged-in string literal would silently skip the concept remap the same way T-001's `0xaebbc6` vapor literal did — the failure mode this guard was added for. Widen the pattern to cover `#rrggbb`/`rgb(a)` strings.

Verified clean (the rest of the checklist):

- Merge faithfulness: `git merge-base main task/T-010` == main HEAD `cb321a6`, and the diff vs main touches only palette files (10 render modules, palette.js, pathcheck, the capture rig, docs, artifacts) — T-004/T-012/T-007/T-015 content is preserved, nothing reverted.
- Gate: `node tools/pathcheck.mjs` in the worktree → **800 passed, 0 failed** (main baseline 775). The pathcheck diff has **zero deleted lines** — purely additive, no assertion weakened or retimed to get green. The previously failing "palette: classic table is byte-faithful to CONFIG.palette" now passes on the merged tree, and the collision that caused it is guarded structurally: every kind in the sim ENEMY roster must have a body token in both tables.
- Classic byte-identity beyond what the assertion can cover (tokens that were literals, not CONFIG.palette reads): limb ladder 8/8, transform ladder 7/7, `rain 0x9fb4c6`, `vapor 0xaebbc6`, `hemiSky 0xcfd8e3`/`hemiGround 0x3a3f46`/`sun 0xffffff`, `muzzle 0xffffff`, `capsuleInk '#14181e'`, `solid = CONFIG.palette.groundAlt` — all match the pre-palette literals exactly. palette.js's `document.body.style.background` write is an identity in classic (index.html:8 already carries `#232830` = `CONFIG.palette.bg`). The hook* keys are the only CONFIG.palette keys skipped by the fidelity assertion, correctly, per decisions entry 5.
- Layer purity: zero `src/pure/` or `src/sim/` changes. palette.js is render-side, Node-safe (DOM write guarded by `typeof document`), and remains the one render module the harness imports; `mode.js`'s `__HB_QUERY__` path keeps the headless import on the concept default (asserted).
- Determinism: no RNG, clock, or 2D-sim surface touched.
- Verdict compliance: `?hook=1` untouched and deliberately still reading `CONFIG.palette.hook*` (entry 5); no geometry choreography touched (entry 3); FAR default and frozen jump/movement constants unchanged; captures taken at `?view=far` (entry 7).
- Perf: no new per-frame allocations — pose objects still reused, instance colors still uploaded only on spawn/type change, `atmosphereBg` is two object lookups per transition frame; `BASE_COLORS = PAL.limb` is read-only aliasing.
- Scope/hygiene: no runtime deps (capture rig reuses tools/playtest's playwright-core), no build step, capture rig has zero effect on the shipped game and carries an honesty note (schedule-matched, not frame-locked; traversal-action is event-triggered), 1.8 MB of PNG evidence under the existing artifacts/ precedent, no OSTK artifacts. Feel judgments correctly deferred to the operator.
- Not re-litigated: the concept palette shipping as the DEFAULT (with `?palette=classic` as the opt-out) rather than behind an off-by-default flag. That shape was approved at pass 1 and is defended as documented DESIGN canon under decisions entry 8's delivery mandate, not as an unjudged mechanic — it belongs to the operator packet, not this gate.
- Integrator bookkeeping (not a builder defect): SPRINT.md Inbox issue I-004 ("ENEMY acid-green role lands only partially") is resolved by this branch's hostiles wiring and should be closed at merge.
