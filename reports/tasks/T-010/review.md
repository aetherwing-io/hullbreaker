APPROVE

src/render/hostiles.js:73 — MINOR (carried from pass 1, still valid): the acid-green ENEMY role does not land on hostile meshes this pass — hostiles.js still reads CONFIG.palette (grey-box greens) because T-004 owns that file and is still `doing` on main (verified: SPRINT.md line 82, and hostiles.js is one of only two remaining CONFIG.palette readers, the other being the judged-rejected hook.js). The tokens are authored and pathcheck-asserted in palette.js, and the deferral is disclosed in the palette.js header, docs/DESIGN.md, and the SPRINT operator packet. After T-004 merges, the one-line wiring (point hostiles.js at PAL.wasp/carrier/hound/houndTell/houndCharge) must land and the pathcheck exemption must be removed.

(worktree base) — MINOR, informational: main advanced again during review (1cd3dbc → 1154b46, the T-007 docs-sync and T-015 asset-pipeline merges). Unlike the pass-1 stale-base MAJOR, this delta touches no `src/` file — README/SPRINT/docs/reports/tools only — and `git merge-tree --write-tree 1154b46 HEAD` resolves clean with zero conflicts, so the certified runtime code is exactly what merges. No re-work needed; the normal merge gate suffices.

Pass-1 findings verified addressed: (1) MAJOR stale base — fixed by merge commit 0c4c003: branch now contains T-001/CP3 v3, the PAL tokens are re-applied to the reworked transform renderer (the diff vs main is palette-only there), T-001's vapor literal 0xaebbc6 is tokenized (CLASSIC byte-faithful, CONCEPT rides the teal atmosphere family, both asserted), node tools/pathcheck.mjs passes on the merged tree (692 passed, 0 failed, assertions purely additive over main's 670), and the transform-boot pair was re-captured post-merge (artifact bytes changed in 0c4c003; the committed pair shows the CP3 v3 slice in both palettes). The fix also added a mechanical guard: tokenized render files now reject raw hex color literals (0xffffff identity base excepted), so the next merged-in literal fails pathcheck instead of silently skipping the concept remap. (2) MINOR hostiles exemption — re-checked, T-004 still in flight, deferral stands (carried above). (3) MINOR capture honesty note — fixed: tools/playtest/README.md now states the traversal-action scene is event-triggered and its two sides can fire seconds apart, matching what the committed pair shows.

Clean elsewhere: no src/pure or src/sim changes (layer purity and determinism untouched); palette.js is render-side, deliberately Node-safe, and documented as the one render module pathcheck imports; fog and background come from the single bg token by construction and are asserted; CLASSIC stays byte-faithful to CONFIG.palette (asserted) so ?palette=classic cannot drift from the grey-box baseline; ?hook=1 untouched and its colors deliberately untokenized per decisions entry 5; FAR default and frozen movement constants untouched (all captures at ?view=far); no new runtime deps (capture rig reuses tools/playtest's playwright-core); no per-frame allocation or instancing regressions (instance colors still set only on spawn/type-change); all four scene groups in artifacts/palette-v1/ carry genuine side-by-side pairs and the operator packet is queued in SPRINT with exact URLs and boards-01/10/13 questions — enemy-color gap disclosed in the packet. Feel judgments (does the rust read as the boards' Meridian, do threats pop at FAR) correctly deferred to the operator.
