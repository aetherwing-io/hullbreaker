APPROVE

- tools/pathcheck.mjs:9276 (assertion) / src/render/fx.js:271 — the new "worst-case spark stretch stays under 1.5x its own size" guard checks `travelStretch(spec.speed)` against the burst's *nominal configured* speed only. It ignores that `row.vy += row.gravity * dt` accumulates every frame for the particle's whole `ms` lifetime, so the true worst-case runtime speed (a particle launched straight down at max burst magnitude, gravity accelerating it further for its full life) is materially higher than `spec.speed`. Computed independently: death's true worst-case runtime speed is 14.22 tiles/s vs the nominal 7.5 the assertion uses, and the resulting stretch (0.237 tiles) is 92.9% of the 1.5x-size cap (0.255), not the ~49% the assertion's own math implies (impact 82.0%, hurt 78.6%, pickup 47.2% — all still pass today). Nothing is broken now — the true worst case naturally coincides with `particleAlpha(u)` fading near zero as `u→1`, so it's optically masked — but the assertion's docstring claim ("guards every currently-tuned effect… against a future retune quietly reintroducing the smear") is broader than what it actually verifies: a retune of `gravity` or `ms` alone (not `speed`/`size`) could push a burst's true on-screen stretch past the cap while this gate stays green. This is the project's own named failure class (an assertion whose subject is the configured value rather than the observable worst case). Non-blocking because the load-bearing gate — `bulletNoseTiles()`'s unconditional `Math.min` clamp against `BULLET_NOSE_CEILING_TILES`, which is what actually stands between a drawn shape and a claimed-hit-the-sim-never-gave — has no such gap (verified below); this only affects the cosmetic spark-stretch cap, not sim-collision truth. Worth a follow-up assertion that computes worst-case speed as `spec.speed + |spec.gravity| * spec.ms/1000` (launched straight down) rather than `spec.speed` alone.
- reports/tasks/T-041/build.md:150-160 — the report claims the arity-reintroduction break produced "15 failures"; re-running the identical edit produces 18 FAIL lines (verified twice, clean before/after). Not a defect — the gate result is strictly stronger than claimed, just a miscount in the report, not a rerun-affecting one.
- reports/tasks/T-041/build.md (Open feel questions) — doesn't mention that this pass was tuned and reviewed against the pre-T-035 flat build: the branch's base (d3f6628) predates `docs/decisions.md` entry 14 (T-035's value ladder shipping at half dose by default). A darker default world changes how a spark/bullet streak reads against its background. Flagging for the operator, not the builder: when judging S10's feel, note it was authored/measured before the value-ladder dose landed and may want a re-look once T-035 merges. No action needed from this lane.

Verification performed (all reproduced independently, not inherited from build.md):
- src/pure/juice.js is zero-import, contains no `THREE`/`document`/`window`/`Math.random`/`Date.now`/`performance.now` (read in full; pathcheck's generic `guardLayer('pure', …)` static scan also covers it, confirmed by inspecting tools/pathcheck.mjs:163-188).
- `node tools/pathcheck.mjs` → 1763 passed, 0 failed, matching the report.
- Broke and restored three times, worktree confirmed clean (`git status --short` / `git diff --stat`) after each: removing the `Math.min` clamp in `bulletNoseTiles()` → 4 FAILs (L nose grew to 1.7867 tiles, matching the report exactly); reintroducing an `(speed, ms)` signature on `travelStretch()` → 18 FAILs (NaNs cascade through every downstream check, per above); adding `const blast = GLYPH_GAIN;` to `src/render/bullets.js` → 2 FAILs (both halves of the extended damage-prop GAIN guard fire).
- Re-ran `tools/playtest/juice-stress.mjs` myself (ephemeral port via the tool's own `port: 0`, none of 8741-8744 touched): fps 119.9, avgMs 8.34, worstMs 9.3, over20ms 0, liveProjectiles 256/256, sparks 224/224 — matches the report's "after" row within the claimed 0.1ms noise band.
- Independently measured `renderer.info` (draw calls/triangles/geometries/textures) at 1280x800, 1.5s after boot, on both `main` at the branch's merge-base (d3f6628, via `git archive` to a scratch copy) and this worktree: 97 calls / 50,180 tris / 54 geometries / 5 textures on both — zero delta, confirming the "zero new draw calls" budget. (Absolute counts differ from build.md's 94/50,196/58 and the proposal's 101 baseline — different capture timing/conditions — but the paired before/after comparison is what the budget claim requires, and that delta is exactly zero.)
- `src/config.js` diff is exactly one delimited, appended block (`BULLET_NOSE_CEILING_TILES`); no other line in the file changed; no jump/movement constant touched.
- `git diff main...HEAD --stat` (three-dot, authorship-only) matches `git show --stat 0132aa2` exactly: 6 files, 469/-24 — confirms no phantom hunks from `main` advancing.
- No new raw hex introduced in `src/render/fx.js`/`src/render/bullets.js` (the pre-existing `0xffffff` white-base-material lines in bullets.js are outside every diff hunk, unchanged by this pass).
- `bulletNoseTiles()`'s tile/scale-factor math checked by hand against `bulletMesh`'s base `SphereGeometry(CONFIG.rifle.radius, 6, 6)`: `base[0]*radius` → tiles → clamp → `/radius` → scale factor is dimensionally consistent, and the per-weapon table in build.md (R 0.593, S 0.511, L 1.120 unchanged, H 0.412, F 0.393/0.407) reproduces correctly from `CONFIG.weapons` + `bulletNoseTiles`.
- `b.crawling` is sim-initialized `false` for every bullet and only ever set `true` for `type==='F'` (src/sim/weapons.js:25,46,133), so `bullets.js`'s `const crawling = b.type === 'F' && b.crawling` and its `def.crawlScale`/`CONFIG.weapons.F.crawlSpeed` reads can't be hit for any other type — no undefined-access risk from generalizing the branch to every weapon.
- No `?hook=1` reference anywhere in the touched files; the S10 spark-stretch code lives entirely inside the pre-existing `JUICE_ENABLED` gate (`?juice=0` already zeroes it out), so the A/B this rides is the one the build report names, not a new flag.
