REQUEST_CHANGES

src/ui/audio.js:468 — MAJOR: per-face ambience layering is off by one and drops the final layer, against the acceptance criterion "per-face ambience layering audible across corner rituals." `view.level.faceRevealed` is fired from `revealFaceRest` inside `finishCorner` (src/sim/wavegate.js:72-83) BEFORE `c.state = 'done'`, so the `applyLayers()` at onFaceRevealed counts the completing corner as not-done (`layerTarget`, src/ui/audio.js:289 counts `state === 'done'` only), and `onCornerFinished` (src/ui/audio.js:467, which does fire after `'done'`) plays sfxResume but never calls applyLayers. Net effect: the first corner ritual gains no ambience layer, every subsequent layer swells in one face late, and the last face's layer never enters the mix (nothing calls applyLayers after the final corner). The transform path is correct because sim/transform.js:203-210 sets `state = 'done'` before its finished hook — the fix is one line: add `applyLayers()` to onCornerFinished.

reports/tasks/T-012/ — MAJOR: no builder report exists (neither in the main checkout nor the worktree), but the task's verify line requires a "manual listen note in report." There is consequently no listen evidence for the SFX recipes or the layering, no smoke-suite result recorded, and no open feel-questions list for the operator (CLAUDE.md mechanic/feature DoD). The audible-behavior acceptance boxes cannot be checked from code reading alone.

src/ui/audio.js:447 — MINOR: onCapsuleRemoved re-derives "pickup" purely from `circleHitsPlayer` overlap, but sim removal also happens on expiry/cull (src/sim/capsules.js:71,78 — killY, `dieAt`) and teardown; a capsule expiring or being cleared while overlapping RIG (e.g., a just-dropped capsule inside the recatchMs window dying under the player) plays a false pickup chime. Cosmetic only; gate on catchability too, or document it as an accepted edge.

src/ui/audio.js:34 — MINOR: the header claims folding the AUDIO tuning block into CONFIG "is a recorded follow-up for the integrator," but no such record exists in SPRINT.md (main or worktree). Record it in SPRINT's Inbox or reword the comment — comments must not claim bookkeeping that was not done.
