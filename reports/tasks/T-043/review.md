APPROVE

- SPRINT.md (2026-08-02 OPERATOR GOAL CHANGE block, in main's SPRINT.md) / reports/tasks/T-043/runs/T-043-{before,after}-sixface/summary.md — the build report's own single-run six-face evidence trends toward *easier*, not merely more legible: kills 12→18, run length 59.7s→56.6s, same 3 lives spent, same gate reached (scroll 140), same fixed reflex policy both sides. Nothing in the diff moves a named roster-pressure constant (`diveRange`/`diveSpeed`/`diveMs`/`diveCooldownMs` in `src/config.js` are untouched — confirmed by `git diff main...HEAD --stat -- src/config.js` returning empty), and the mechanism — a stationary, correctly-aimed, glowing "committed" pose the player can shoot during — is not new; it matches the hound/polyp/mortar tells that already ship on by default (T-003) and are already exploitable the same way (a body sitting in its warning pose is a free shot at every one of them today). Given the operator's live, explicit "DURABILITY outranks difficulty tuning. Do NOT tune the difficulty curve against this goal" directive, build.md should have named that directive against its own kill-count evidence; it never mentions it. **Not blocking** — the constants are unchanged, the mechanism mirrors an already-approved roster pattern, and the report is honest that one run each side is not a statistic (§3.3 says so explicitly) — but the integrator/operator should read this evidence consciously as a possible difficulty-adjacent side effect of a legibility fix, not wave it through as pure legibility. Route it alongside build.md §5's four feel questions rather than resolving it in code.

- reports/tasks/T-043/build.md:~34 ("was 1742 before this task's 18 new assertions") — checked independently by building `git merge-base main HEAD` (69e1f906262cdebd4bbc7f83f0dd27885e8baa92) into a scratch worktree and running `node tools/pathcheck.mjs` there: base is **1741**, not 1742 (reproduced twice, stable). Traced the extra pass: it is not a miscount of the T-043 block itself (that block statically contains exactly 18 `ok(` calls, confirmed by `git diff main...HEAD -- tools/pathcheck.mjs | grep -c '^\+ *ok('`, and all 18 execute) — it's a pre-existing generic per-file loop at `tools/pathcheck.mjs:6181` (`for (const file of [...layerFiles('pure'), ...layerFiles('sim')]) ok(!stripComments(...).includes('legibility'), ...)`) that iterates every file in `src/pure`/`src/sim` and now iterates one more because `src/pure/wasp.js` is new — one incidental, correctly-passing assertion neither authored nor claimed by this task. Total (1760) and the 18-new-assertion claim for the T-043 block are both right; only the stated "before" baseline is off by one. No gate impact, purely a reporting nit.

- No blocking findings on layer purity, determinism, scope, or durability. Notes below are why.

Verification performed (all reproduced independently, not inherited from build.md):
- `src/pure/wasp.js` has zero imports and no `THREE`/`document`/`window`/`Math.random`/`Date.now`/`performance.now` (read in full; `grep` confirms). `guardLayer('pure', …)` in `tools/pathcheck.mjs` iterates `readdirSync` over `src/pure/`, so the new file is automatically covered by the static cross-layer-import and banned-token scan, not specially allow-listed — confirmed by reading `tools/pathcheck.mjs:161-197`.
- `src/sim/hostiles.js`'s diff (`git diff main...HEAD -- src/sim/hostiles.js`) touches only the wasp `cruise`→`dive` commit branch, the `dive` movement branch, and `clearHostiles()`; no other kind's code path, and `lockUntil` is a field no other kind reads (`grep -n lockUntil` hits only the three wasp-path lines) — confirmed the `cruise`/`dive`/`recover` state machine is unreachable for hound/polyp/mortar/carrier (each is dispatched to its own branch above it, or the ROOTED-kind checks) so there's no collision risk from the new per-hostile field.
- `node tools/pathcheck.mjs` → **1760 passed, 0 failed**, run 3× back-to-back with byte-identical stdout (`diff` clean across all three), including the two new live-sim child processes whose output embeds exact floating-point numbers — real evidence of determinism for this change, not just a stable pass/fail count.
- Broke and restored twice myself (separate from the builder's three): `WASP_DIVE_LOCK_MS = 0` → exactly the 3 FAILs build.md claims, same messages, `(0 frames checked)` for the lock-window check; `WASP_SQUAD_STAGGER_MS = 0` → exactly the 2 FAILs claimed, same cluster-timestamp printout (`[916.667,916.667,916.667,916.667]`). Restored both times; `git status --short` and `git diff --stat` clean after each, `pathcheck` back to 1760/0.
- Confirmed no softlock risk from the new timing: the `dive`→`recover` transition still has an unconditional `gameMs > e.stateUntil` exit alongside the new `launched &&` floor check, so a wasp can never get stuck in `dive` forever regardless of the lock — this matters given the live durability-over-difficulty mandate.
- `clearHostiles()` (only caller: `src/main.js:214`) resets `lastWaspLockMs = -Infinity`, so the squad clock cannot leak across a fresh run; within one continuous run it is a single persistent clock (by design, matching the module comment), not reset per-wave.
- `git diff main...HEAD --stat`: only `src/pure/wasp.js` (new), `src/sim/hostiles.js`, `tools/pathcheck.mjs`, and `reports/tasks/T-043/**` are touched — no render/UI/config/SPRINT/CLAUDE.md scope creep.
- Re: no query flag — this worktree's `docs/decisions.md` tops out at entry 13 (branched before entry 16 landed on main on 2026-08-02; correctly left untouched per lane discipline). Checked entry 16 directly on **main**: "Blanket off-by-default flags… is retired as a blanket rule… anything he has approved ships ON by default with an escape hatch back" — real, and it supports shipping this unflagged, independent of build.md's own (also valid) T-003 precedent argument.
- Ports: never touched 8741-8747 or the builder's scratch 8760 (confirmed already killed — no listener). All my verification ran headless `node` (pathcheck + scratch child processes) and a detached scratch git worktree for the merge-base measurement; no server of my own.
