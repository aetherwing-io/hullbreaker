APPROVE
README.md:100 — MINOR: "the wave-4 delivery queue covers these" overstates. "These" grammatically includes the boss and the flight interlude, but SPRINT.md's queue and delivery target cover only polyp, mortar, juice, menus/shell, and WebAudio; docs/FLEET-PLAN.md:350 in this same diff correctly records flight as still out of scope and the Crown finale as not yet queued, so the two files contradict each other. Suggested fix: scope the pointer ("SPRINT.md's wave-4 queue covers the enemies, juice, shell, and audio; boss and flight remain in DESIGN's sequence only").
tools/playtest/lib/sampler.mjs:40-47 — MINOR (pre-existing, untouched by this diff, outside the docs lane): the inline comment "testapi does not expose hostiles/capsules at all as of this writing" is now half-stale — testapi's telemetry() does publish hostiles[] since e7b2952 (capsules still HB-only). The updated README limitation #7 documents the true current state, so this is a noted harness-side follow-up, not a defect of this diff.

Verified clean:
- node tools/pathcheck.mjs in the worktree: 620 passed, 0 failed.
- Diff touches only README.md, docs/HANDOFF.md, docs/FLEET-PLAN.md, tools/playtest/README.md — docs lane only; no runtime, no deps, no OSTK artifacts, no test/assertion changes, frozen constants untouched, ?hook=1 untouched.
- Hook request #2 claim checked field-for-field: src/main.js telemetry() publishes hostiles[] {id, kind, state, dir, x, y, hp, materialized}; introduced by 2732563, an ancestor of merge e7b2952 as the README states; root README "Debug handles" (line 181) documents it; HB.snapshot() spreads telemetry() so the channels cannot drift, as claimed.
- Hook request #3 claim checked: HB.score = {enabled, events, snapshot, reset} exists in src/main.js; SCORE_ENABLED = QUERY.get('score') === '1' (src/mode.js:64) and every event path in src/sim/score.js early-returns when disabled — "events flow only under ?score=1, inert otherwise" is accurate; harness-side proxies honestly recorded as still open.
- Limitation #7 / hook #8 claims checked: lib/sampler.mjs still enriches hostiles from window.HB.snapshot() (lines 113-127), so the narrowed caveat is honest; transform block carries tMs/progress/frontierX/sealX plus the T-002 decisions trace (src/main.js:258-281), corner block present.
- HANDOFF claims checked: merge-base of task/T-007 is exactly 15de009, so the recorded HEAD was accurate at write time; tools/simlab/, docs/ORCHESTRATION.md, docs/playtests/2026-07-t2-frame-alignment.md, and .claude/agents/asset-artist.md all exist; the T-002 finding summary (bit-deterministic under frame-scoped input, residual nondeterminism in browser input delivery, hook #5 endorsed and not yet built) matches the finding doc verbatim; T-005 fixture import confirmed (lib/fixture.mjs re-exports TRAVERSAL_FIXTURE from src/pure/traversal.js).
- FLEET-PLAN entry-8 record checked against docs/decisions.md entry 8: verbatim operator quote matches; autonomous-merge, codex-CLI asset lane, six-face-hold release, fun-oracle retention, and the four surviving agent/traversal-* branch refs all match; merge-task.sh really enforces reviewer APPROVE + playtester PASS + worktree pathcheck + deterministic smoke + post-merge pathcheck with auto-revert.
- README houndframe claims checked: ?hound=1|2|2.5|3 resolve in src/mode.js, hound 2.5 baseline matches decisions entries 4/6, and src/sim/spawner.js has no hound spawns (not in the default run), as stated.
- No invented decisions found; every recorded verdict traces to decisions.md entries 0a-8 or to shipped code.
