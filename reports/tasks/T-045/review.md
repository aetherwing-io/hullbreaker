APPROVE

Findings (informational; none block merge):

- tools/playtest/README.md, artifacts/scale-v1/*.png, reports/tasks/T-045/build.md were still uncommitted (working-tree only) at the start of this review — the same "committed code before evidence" race the dispatch note warned about. Resolved during review: the builder landed commit 7f1f534 ("T-045: evidence — paired before/after captures, playtest summaries, build report") mid-session, so the branch tree reviewed here is fully committed and `git status --short` is clean.
- src/config.js:83 (T-045 build report), decision-packet reviewer brief item 7 — the build report measures real GL draw calls per frame at 3 story moments (100–103, before≈after) but does not re-run the dedicated 200+-projectile stress harness (tools/playtest/juice-stress.mjs). Low risk given the mechanism (one-time bake at boot, same 8 static InstancedMesh objects before/after — scene-graph node count is unchanged, so per-frame matrix-world traversal cost is unchanged too), but it's a gap against what was asked; worth a follow-up note rather than a re-open.
- src/pure/limb.js:242-244 `sisterBottom` clamps the diagonal's low end to `S.y0` via `Math.max`, which is correct for the play-band fence but means the "diagonal" silhouette flattens at its low end on facets where `jitter` picks a small `base`. Cosmetic only (asserted geometry stays fenced either way); an operator question, not a defect.

Verification performed (all reproduced independently, not inherited from the build report):

1. Depth staging — real: rendered artifacts/scale-v1/0{1,2,3}-*-{before,after}.png show a flat single teal wash pre-change vs. three distinct planes post-change (near rust sister-limb with visible ladders/hatches, mid teal drum spine, distant stepped teal far-body silhouette) at the shipped FAR view. Not merely code that exists unused — visually confirmed in all three captured moments.
2. Human-scale marks — placed where seen, sized against RIG: `CONFIG.player.height`-derived door/rung/rail dimensions (config.js:656-670), emitted on both the hull skirt under RIG's feet and the backdrop sister limb at byte-identical absolute dimensions (pathcheck.mjs section 4, all green), visible in the captures directly beneath and behind RIG rather than tucked in a corner.
3. Static anatomy — `src/pure/limb.js` has no time argument anywhere, no `Math.random`/`Date.now`/`performance.now` (grepped clean), only `jitter()` (a deterministic hash). `src/render/limb.js`'s `bakeLimb()` runs once at module evaluation (`export const limbPieces = IS_G1 ? bakeLimb() : 0;`), no per-frame/ritual/build hook added. Every backdrop piece has `depth < 0` (behind the plane), satisfying `limbPlanViolations` by construction, confirmed by full pathcheck run.
4. Entry 14 (no re-darkening) — tier separation comes from a hue split (rust `hull`/`rib`/`scute` tokens for the near sister limb vs. teal `wall`/`shadow`/`skyline` tokens for the mid/far tiers, src/render/limb.js MATERIAL_FOR + src/render/palette.js CONCEPT.limb, both pre-existing, unmodified) plus the fog ladder, not from lowering values. `scale-capture.mjs measure`'s own table shows the sky band's median luminance is unchanged (78.3 before and after) — the pass adds mass in front of the sky, it does not darken the sky itself.
5. Determinism/purity — `git diff main...HEAD -- src/pure/limb.js`: no THREE/document/window, no upward imports (only `../config.js`, `./path.js`). `node tools/pathcheck.mjs` in the worktree: 1853 passed, 0 failed, including the static layer-purity guards.
6. Frozen constants — `git diff main...HEAD -- src/config.js` shows the entire diff lands after the `tone` array inside `CONFIG.limb`; `CONFIG.player` (jump/movement block) is untouched.
7. Draw calls/perf — verified structurally, not just by report claim: `src/render/limb.js` MATERIAL_FOR maps all 13 new kinds onto the same 8 pre-existing material keys (`hull, wall, rib, machine, shadow, scute, scuteAlt, skyline` — cross-checked against `src/render/palette.js`'s unmodified `limb: {}` table, which has exactly those 8 keys). One InstancedMesh per material key means 8 draws before and after — pathcheck asserts this (`usedNow.size === usedLegacy.size`) and I independently broke it (gave `bdSpire` a 9th material) to confirm it goes red, then restored. `scale-capture.mjs`'s real WebGL-wrapped draw-call counts (100–103 both variants, ±1 tracking live hostiles/projectiles, not the limb) corroborate.
8. On by default — `SCALE_PASS = QUERY.get('scale') !== '0'` (src/render/limb.js:83), default true, `?scale=0` escape hatch restores the old two-slab `CONFIG.limb.silhouette` plan verbatim (asserted).
9. New assertions bind — I independently broke one claim myself (`CONFIG.limb.backdrop.sister.depth: -14 → -1`, pushing the fog factor to -0.018) and reran pathcheck: 6 of the new assertions failed with on-target messages (fog band, near/far comparison-object existence, dimension match, play-band/hull-skirt containment); reverted with `cp` back to the original file, reran clean (1853/0), confirmed `git status --short` had no leftover diff from my own edit. The build report's own break-log (8 separate breaks, each producing a distinctly-worded FAIL) is consistent in style with what I reproduced and is credible.
10. pathcheck count reconciliation (per dispatch note) — merge-base `cad82ed` alone: 1812 passed. This branch's own tree (base + only its authored diff, since the three-dot diff for `tools/pathcheck.mjs` is purely additive: 223 lines added, 1 line changed to extend an import, 0 assertions removed): 1853 passed — a genuine **+41**, matching a manual count of every `ok(...)` call added, including loop multiplicities (16+6+3+9+7=41). `main` HEAD (`0d98c70`) also happens to read 1853, but from unrelated commits (T-037/T-039/T-041/T-043 work merged after this branch forked) — the coincidence flagged in the dispatch note is real but benign: T-045's 41 assertions are genuinely additive on its own base, not phantom or double-counted. The integrator will still need ordinary pathcheck splicing at merge time since main has moved substantially (many files this branch's two-dot diff shows as "deleted" — T-037 split experiment reports, `tools/pathcheck-split.mjs`, etc. — are main-side churn unauthored by this branch, not something T-045 removed).
11. Scope/hygiene — diff touches exactly `src/config.js`, `src/pure/limb.js`, `src/render/limb.js`, `tools/pathcheck.mjs`, `tools/playtest/scale-capture.mjs`, `tools/playtest/README.md`, plus its own `reports/tasks/T-045/*` and `artifacts/scale-v1/*` — matches the task's named files. No `package.json` touched (playwright-core was already a dev dependency). No `SPRINT.md`/`CLAUDE.md`/root `README.md` touched.

Operator questions (build.md already queues these; not blockers for this review):
- Does the sister-limb tier read as another arm of the same creature, or as floating scenery?
- Does the visible gap of clean haze between the wall top and the backdrop read as distance, or as the backdrop hovering?
- Is a visible Crown silhouette (spires) wanted mid-run, or only at the climb's end?
- Three tiers, or fewer — is the vertebral-drum tier (62% haze) worth its cost?
