APPROVE

## Findings (most severe first)

- `tools/pathcheck.mjs:9237-9463` / `reports/tasks/T-044/build.md` (scope/hygiene, not a blocker) — the branch still carries its T-044 assertions in the pre-T-037 `tools/pathcheck.mjs` monolith; `main` has since split to `tools/pathcheck/` modules (confirmed: this worktree's `tools/pathcheck.mjs` is 523k and lacks the `GENERATED BY tools/pathcheck-split.mjs` banner main's tree carries). The build report already flags a three-way-merge risk for T-021's overlapping surface but not this monolith/module split. The worktree also has a stray **untracked** `tools/pathcheck/t-044-corner-reveal-set-pieces.mjs` (14k, not staged, not part of this diff) — looks like debris from a prior migration attempt on this file; it isn't committed and isn't covered by `git diff main...HEAD`. Neither blocks approval; the integrator needs to run the monolith→module reconciliation (`migrate-lane.mjs` or equivalent) before merge, same as any other pre-split lane, and can clear the stray file at that time.
- Everything else checked below came back clean.

## Verified clean

**Blocker 1 (previous review): false "hostiles LIVE" framing — FIXED.** Re-read `tools/pathcheck.mjs:9365-9461` in full. Every comment and assertion message now says "TERRAIN ONLY" / "hostiles removed every frame" / "route, not fight — same idiom as T-009" / "says nothing about combat" / explicitly calls out the old framing as wrong ("must never be read as 'hostiles live' (review finding, T-044 cycle 1) ... which was false — fixed here, assertions unchanged"). The assertions themselves are unchanged in substance from before (bridge-survival fingerprint, state PLAYING/VICTORY, scroll reaches outro end, `mounted.length >= 3` for real `onOneWay` contact) — only the prose changed, as instructed. `CONFIG.wasp.enterMs` (900ms) vs. the harness's per-frame `removeHostile` loop still means this proves terrain reachability plus real physics contact, not combat survival, and the code/comments are now honest about exactly that.

**Blocker 2: difficulty measurement — sound and honestly reported.** I independently re-derived the report's arithmetic from its own raw table (`build.md:161-166`) rather than trusting the percentages:
- Pooled aimed-policy, all 4 sessions (n=18/side): counting values >140 (a genuine gate-2 clear per the report's own reference marks) gives base 2/18 = 11.1%, branch 6/18 = 33.3% — **matches the report exactly**.
- Pooled *without* the interleaved batch (n=13/side): base 1/13 = 7.7%, branch 5/13 = 38.5%, ratio ≈5x — **matches the report's claimed "~5x the smaller, non-interleaved sample suggested."** The disclosed downward revision from ~5x to ~3x once the interleaved batch is folded in is real arithmetic, not a rhetorical rounding — the report shows both numbers and why they differ rather than quietly replacing one with the other.
- Weak-policy floor/ceiling claims (`75` floor both trees, `140` vs `171.7` ceiling) also check out against the raw values.
- I then ran my own small interleaved check (4 runs, alternating `base, branch, base, branch`, `six-face-aimed-run.json --deterministic --stop-on-game-over`, pinned worktrees: this branch on an ephemeral port, `69e1f90` merge-base checked out to a scratch `git worktree` on another ephemeral port, both killed/removed after): **base 205/140 (1/2 cleared gate 2), branch 140/140 (0/2)** — the opposite direction from the report's pooled lean. This is a tiny sample and proves nothing on its own, but it independently reinforces exactly the point the report is making: run-to-run noise on this metric is large enough to flip the sign in a small interleaved batch, which is why the report treats "how much of this is a real terrain effect vs. session noise" as open rather than picking the cleanest cut. Nothing I found contradicts the report's honesty in either direction — it doesn't claim "no difficulty change" (the terrain measurably moves the pooled clear rate) and it doesn't claim "the arena clearly works" (it explicitly calls the pooled lean "weaker and less certain," names the interleaved-alone null result, and routes the call to the operator).
- Sibling-lane `meta.deterministicDispatch.gameMsMax` bimodality warning: the report explicitly cites it as the reason it ran the interleaved control at all, and is upfront that its own metric (`scrollX`) is a different field than the one the sibling lane flagged while carrying "the same underlying risk" — it does not claim immunity. I checked `meta.deterministicDispatch.gameMsMax` on my own 4 runs; it just tracks each run's actual death time under `--stop-on-game-over`, which is expected to vary with outcome (unlike a fixed-duration script such as the sibling lane's `mid-route.json`), so it isn't informative for this script the same way, and the report is right not to lean on it directly.
- Sample size is stated as what it is (n=18/13/5 depending on the cut) throughout, never inflated or hidden.

**Static-anatomy compliance (entry 3):** ARRIVAL/ARENA are added once inside `buildLevel()` (`src/pure/generator.js:177-189`) at boot time; nothing in the diff touches a per-frame render or update path, and no ground height is ever written (`src/pure/lattice.js`'s `latticeArrivalSites`/`latticeArenaSites` only read `groundH`, `latticeInstallSite` only pushes). This is baked, revealed geometry — compliant.

**Entries 17/19 (not present in this branch's own `docs/decisions.md`, which stops at entry 13 — confirmed the branch predates them and read them from `main`):** entry 17 (FAR stays) isn't implicated by this diff (no camera/render touched). Entry 19 (spread is the feature) is the framing the report correctly adopts, per the analysis above.

**Layer purity:** no `THREE`/`document`/`window` in `src/pure/lattice.js` or `src/pure/generator.js`; both import only from `./rng.js`/`./path.js`/`./traversal.js` (no upward imports). `node tools/pathcheck.mjs`'s static guards raised nothing.

**Determinism:** no `Math.random`/`Date.now`/`performance.now` in either touched pure file; ARRIVAL/ARENA are pure functions of `cfg.gen` and `groundH`. 2D `(s,y)` platform shape (`{x0,x1,y}`) preserved throughout.

**Pathcheck:** reproduced `1759 passed, 0 failed` myself (matches build.md exactly). Re-pinned regression values (`gH.length===445 && plats.length===77`, new fingerprint `0447885a`) are legitimate re-pins with inline reasoning for the disclosed 62→77 platform-count change, in the same style the codebase already uses for prior re-pins (T-009's own 49→62 re-pin sits right above it, untouched) — not weakened assertions.

**Frozen constants / lane fence:** `src/config.js`, `SPRINT.md`, `CLAUDE.md`, `index.html`, `src/render/**`, `src/sim/hostiles.js`, `src/ui/**`, `src/main.js` all confirmed untouched by `git diff main...HEAD --stat`. No new runtime deps; `tools/playtest/package.json`/`package-lock.json` untouched, `t044-capture.mjs` reuses existing `lib/sampler.mjs`/`lib/threat.mjs`/`playwright-core`, imports no game code.

**Break/restore proof:** build.md reports two (clear-then-install regression reproduced + 9 failing assertions; dropping face 4's `perch` tier fails the tier-escalation assertion). I did not re-run these myself (my role changes nothing in the worktree), but structurally confirmed `latticeInstallSite` (`src/pure/lattice.js`) only ever pushes, never clears, and the named regression assertion (`tools/pathcheck.mjs:9281-9285`) targets the exact geometry (`x0===319 && x1===326 && y≈4.35`) the report says the earlier version deleted — the same limitation the prior review noted, unchanged by me.

**Screenshots:** all three PNGs in `artifacts/t044-corner-reveal/` are real 1440x900 captures; `f2-arena.png` shows the claimed multi-band checkered deck, a wasp, and bullets in frame, consistent with the report's own (non-feel) description. Faces 3-6 are honestly disclosed as unverified by screenshot, with the reason (documented combat-AI ceiling, same wall on `main`) stated plainly rather than papered over.

## Operator questions (not blockers, feel is not mine to judge)

Carried over from `build.md` — the report already routes these correctly; flagging only that they're real, unresolved, and correctly not decided by the lane:
1. Whether the measured ceiling-up/floor-holds shape (weaker/less certain than first measured) is acceptable as-is, or whether the floor needs pinning down harder before this ships, and if so whether narrower arena footing is the right lever.
2/3. Whether the face-2 ARENA/ARRIVAL screenshots read as "the ship is fighting me here on purpose" / "the world already existed, revealed" — feel calls only the operator can make.
4. Faces 3-6 are geometry-verified but not screenshot-verified — worth a manual pass or a stronger bot before judging the full escalation.
5/6. Whether ARENA verticality must be proven "never the only path" the way the pocket is, and whether "tallest yet" needs to be literal-Y-monotonic vs. structural (face 6's peak is technically lower than face 5's for this seed).
