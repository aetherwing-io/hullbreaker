APPROVE

Verified independently (not inherited from build.md), worktree `task/T-047` @ 82d6bec, diff vs `main...HEAD` (merge-base 4f967fb, confirmed by `git archive` + a scratch pathcheck run: base 1834/0, worktree 1950/0, +116 all from `tools/pathcheck/t-047-light-rig.mjs`):

1. Layer purity — clean. `git diff main...HEAD -- src/pure src/sim` is empty; no upward imports introduced. `lights.js`/`lightrig.js` are imported only from `src/render/camera.js` and `src/render/scene.js` (checked with a repo-wide grep). `lightrig.js` is THREE/document/window-free by inspection and by the static banned-token guard in `tools/pathcheck/_context.mjs:68`, which scans `pure/`+`sim/` (untouched) — separately, `t-047-light-rig.mjs:262-264` re-asserts the same regex against `lightrig.js` itself. `Object3D.prototype.add` is patched only in `lights.js` (never imported by pure/sim, never imported into the Node pathcheck process), is a no-op for non-mesh nodes, and only runs at mesh-construction time (enemy spawn, mesh add), never in the per-frame path — confirmed by reading `hostiles.js`'s `spawned()` (called once per spawn, not per frame) and `bullets.js`/`fx.js` (bullets/sparks are instanced meshes added once at module load, not per shot). A global prototype patch is unusual but it is scoped, harmless outside `Object3D.add`, and there is exactly one page/one scene per load, so no double-install risk in practice.

2. Determinism — no `Math.random`/`Date.now`/`performance.now` in either new module (grepped, and asserted in `t-047-light-rig.mjs:249-253`). I broke this myself: added `Date.now()` inside `updateLightRig` → pathcheck failed with the exact quoted message ("src/render/lights.js reads no clock — the anatomy stays static (entry 3)..."), restored, `git status --short` clean.

3. Operator-verdict compliance:
   - Frozen jump/movement constants: `src/config.js` diff is a pure append after the existing T-041 block; nothing pre-existing changed.
   - `?hook=1`/`hook.js`: untouched (not in the diff).
   - Entry 14 ("too dark" — do not re-darken): re-measured myself with `tools/playtest/lightrig-capture.mjs` against this worktree (`?light=flat` reconstructs the pre-rig look) and against a real `git archive` export of the base commit served independently: shipped @3000ms mean 72.9 / aboveSky 69.0% / p95 110.4 vs base 69.8 / 50.3% / 89.5 — exact match to build.md's table, confirms brighter mean with more contrast, not a darker frame.
   - Entry 16 (ship ON, A/B escape hatch): confirmed — default resolves to the shipped rig; `?light=junk`, `?light=` and no flag all resolve to `rig` (re-ran `--smoke`, 13/13 modes, 0 errors, SELFTEST PASS).
   - Entry 18's binding condition (60fps under the 256-projectile stress path): re-ran `tools/playtest/juice-stress.mjs` myself against this worktree — control/stress/stress+juice=0 all `over20ms: 0`, avgMs ~8.3 on the vsync-locked panel, matching the report.
   - Readability (pillar 5): correctly left to the operator as a look question (build.md §6 Q2) rather than self-judged. One objective point I did check: telegraph lamps (`lampAttach`) use `MeshBasicMaterial`, which this rig's own policy (`shadowPolicy`) marks unlit → cast:false, receive:false, so a cast shadow cannot visually swallow a tell — that's a real, checkable mitigation, not just an assertion of intent.
   - Static anatomy: the rig re-aims only from the camera's look point and yaw (both already computed by `syncCamera()` for the pose itself), reads no clock — verified per above.

4. Test honesty — reproduced 3 of the 10 claimed break/restore proofs myself, each an exact match to the quoted FAIL text, worktree clean after each restore:
   - key elevation 50°→30°: `FAIL "…a key raked below ~40 degrees closes it (0.004)"`
   - exposure 1.35→0.9: both quoted FAILs reproduced verbatim.
   - `Date.now()` in `updateLightRig`: reproduced verbatim (above).
   Also reproduced the claimed in-lane defect directly: exported commit `bba83bc` (pre-fix) to a scratch tree and ran the smoke check — transform slice caster count is **5/585**, exactly as claimed; the worktree tip is **573/585**. This is a real defect that was found and fixed in-lane, not a retimed or weakened assertion.

5. Perf:
   - Draw-call metric: re-ran the shadow-cost probe myself (`lightrig-capture.mjs --shadowcost`) on the default run and on `?slice=transform` — `renderer.info.render.calls` barely moves when toggling `shadowMap.autoUpdate` (my deltas: -0.1 to -0.7 calls) while 76 (default) or 573 (transform) casters are actually being drawn into the shadow map. This confirms the report's claim: **the project's draw-call metric is now an under-count whenever shadows are on**, and every existing lane's "N draw calls" number is comparing against a metric that changed meaning under it. The report discloses this plainly and files it as a PROPOSED INBOX ISSUE (S3) rather than hiding it — correct handling, but it should be triaged promptly since several lanes cite draw-call totals as their perf budget evidence.
   - Frame cost: my own re-measurement (same tool, same method, vsync disabled) was noisier than the builder's own runs (I got -0.06 to +0.39 ms on repeated default/transform-slice readings, vs. their 0.25–0.68 ms), consistent with the report's own honesty note about machine-noise sensitivity at this scale. Direction and order of magnitude both hold: sub-millisecond, well inside the 16.7 ms budget, and the canonical vsync-locked gate (`over20ms`) is 0 either way.
   - No per-frame allocation in `updateLightRig` (scratch objects reused; the last commit specifically replaces a `for...of` destructure with an indexed loop to avoid an iterator allocation in the hot path — verified via `git show 82d6bec`).
   - No unbounded arrays (`viewLights` is capped at `rig.lights.length`, ≤3).

6. Scope & hygiene — diff is exactly `src/config.js`, `src/render/{camera,scene,lightrig,lights}.js`, `tools/pathcheck/{manifest,t-047-light-rig}.mjs`, `tools/playtest/lightrig-capture.mjs`, `reports/tasks/T-047/build.md`, plus artifacts. No `package.json`/build-step change, no `docs/`, `SPRINT.md`, `README.md`, or `CLAUDE.md` touched (decisions.md entry 18 is the merge-base, not part of this branch's authored diff). No other lane's file touched (hostiles.js, transform.js, palette.js, hook.js all absent from the diff). Flags behave per entry 16 (ON by default, flags are A/B-only).

Minor note, not a blocker: my `git status --short` came back dirty mid-review after I copied this worktree with `cp -r` into a scratch dir and ran `git checkout <base> -- .` there — worktrees share the parent repo's git metadata via a `.git` file, so that mutated this worktree's real index (not its working tree). I caught it via `git diff HEAD` (empty) before touching anything else, ran `git reset HEAD -- <the 4 files>`, and confirmed clean and unchanged. Recorded here for the record; it was my tooling mistake, not the branch's, and the worktree is clean and unmodified at `82d6bec` now. All artifacts I generated during review (`artifacts/lightrig/review-*`) were deleted afterward.
