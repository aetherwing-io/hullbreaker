APPROVE

src/pure/transform.js:382 — MINOR: the flip ritual pins the plate to `ajarFrac` at t=0, assuming the arm swing (unlatchMs+ajarMs = 500 ms) always finished. The guard (tools/pathcheck.mjs:1694) proves this only for a plain sprint at `PL.runSpeed` (531.9 ms window — a 32 ms margin), so any future horizontal speed multiplier active in the slice (surge/flow-style) would bypass the assertion and produce a visible pop from a partial swing to ajar at ritual start. Guarded today; note the coupling for the movement lane.

No BLOCKER or MAJOR findings. Verified in the worktree:
- Gates: `node tools/pathcheck.mjs` 660 passed / 0 failed (main: 620/0 — 40 net new assertions; the removed ones covered only the retired blow/spin debris API, which is now statically banned by pathcheck's `no debris|tumbl` and `bandSlamOffset|zipperOffset` guards). `tools/playtest run.mjs scripts/transform-slice.json --deterministic --max-runtime-ms 20000` → completed, fidelity testapi.
- Layer purity/determinism: no sim file touched; pure changes are math-only; new static guard asserts no sim module references the cover/vapor choreography; no Math.random/Date.now/performance.now in pure/sim; vapor and rain scatter use fixed-seed LCGs in render only.
- Verdict compliance (decisions.md entry 3): bands boot-baked before play, nothing assembles; only the access plate (relocks flush on the hold) and vent cover (blown to a caught stop, hangs forever) move, both persisting — the v2 vanish/9-piece debris tumble is gone; every cover beat is asserted onto the camera's detents; the camera ratchet curve itself is untouched. Vapor is atmosphere, cleared before snap 2 (asserted), and its read is explicitly posed to the operator (SPRINT question 2). `?hook=1` untouched; frozen jump/movement constants untouched (config diff confined to CONFIG.transform.cover); FAR-default respected — the fog now rides `activeCameraDepth()` exactly as camera.js's documented depth-delta rule prescribes for every other mode, with the transform slice's fog ownership already delegated to this module (src/render/camera.js:60).
- Test honesty: playtest script not retimed; new pure logic (transformCoverAjar, transformVapor, reworked transformPanelState) has dense new assertions incl. relock-inside-hold, catch-on-detent, monotonicity, persistence, and no-altitude-granted checks; capture-tool beat-naming caveat is documented (tools/playtest/README.md honesty note; true tMs recorded in artifacts/cp3-transform-v3/index.json, errors: []).
- Perf: vapor is one InstancedMesh (120 motes, one draw call), updated only while a breach ritual runs; scratch objects reused; no per-frame allocations added.
- Scope: diff stays in the CP3 lane (transform pure/render/config, pathcheck, dev-only capture tool with its own README entry, artifacts, DESIGN/SPRINT sync); no new runtime deps; no OSTK artifacts; DESIGN.md records "implemented, judgment pending" without self-declaring fun; operator packet (URL + 5 questions) queued in SPRINT.md's checkpoint queue with the 15-frame sequence + webm + equivalence report in artifacts/cp3-transform-v3/.
