APPROVE

SPRINT.md:274 — MINOR: the CP4 operator packet is written and ready (URL + 5 questions in docs/proposals/2026-07-cp4-default-run-score-setback.md, "CP4 operator packet") but the worktree does not touch SPRINT.md, so the acceptance item "CP4 operator packet queued" completes only if the integrator appends it to the Operator checkpoint queue at merge, per the T-001 convention ("SPRINT: T-001 done (CP3 v3 packet posted)"). Do not let it drop.

tools/playtest/README.md:469 — MINOR: the new real-source protoScore is scoped to the segment since the last score reset (resetScore runs in resetGame, src/main.js:161), i.e. the FINAL attempt on a retrying fixture run, whereas the retained proxy path scores the whole trace. The A.5 section's real/proxy comparison doesn't state this scope difference; one honesty line there would keep pre/post numbers comparable on multi-attempt slice runs. Harmless for the 30 s default-run evidence scripts (no resets observed).

src/ui/hud.js:121 — MINOR: a default-run fallback has no center-HUD callout (the "HULL FALLBACK · LOWER ROUTE" line is ACTIVE_SLICE-gated). Correctly lane-fenced and disclosed in the recommendation ("Known gaps") and surfaced as CP4 question 5 — but it is a pillar-5 readability gap that needs a filed HUD-lane follow-up task so it isn't lost if the operator adopts the composition.

Checks performed:
- node tools/pathcheck.mjs in the worktree: 693 passed, 0 failed (matches the doc's claim). New assertions are purely additive: run-tune halving relationship, run-fallback tune equality with the slice tune, and two headless default-run children (flags on: run tune prices +14, fallback keeps lives/control, streak ceiling spends a stock life, A.5 setback envelope; flags off: meter inert, stock lives path byte-identical). No existing assertion weakened or deleted.
- Layer purity/determinism: no THREE/document/window and no banned time/random sources in touched pure/sim files; the one new cross-layer import (src/sim/player.js -> src/pure/score.js) is downward; RUN_FALLBACK_ENABLED follows the existing mode.js query-flag pattern; all tunes are frozen static data.
- Verdict compliance: entry 0a defended, not re-litigated (lives survive only as the escalation ceiling, explicitly queued as CP4 question 3); both flags off by default in the run and proven so; slice default behavior preserved (?fallback still on-by-default there via SLICE_FALLBACK_ENABLED, retry ceiling unchanged); ?hook=1 untouched; CONFIG/jump constants untouched; FAR default untouched; no self-declared fun — recommendation ends in an operator packet.
- Test honesty / evidence: the recommendation's numbers match the actual run artifacts in tools/playtest/runs/ (protoScore 600.5 real / 721.6 proxy baseline / -117.2 nojump; THREAT 920; 3 setbacks, 0 deaths; final x 89.25 vs 59.65; consoleErrors/pageErrors 0; deterministic:true recorded in report.json). scored-run vs scored-run-baseline verified byte-identical inputs except the URL flags. mid-route and transform-slice smokes completed. SCORE_RUN verified against the proposal's published A.3 tables (gains and -7/-22 drain) exactly.
- Perf/scope: only a per-frame Math.min added to the hot loop (minEdgeMargin in all modes — telemetry-only, HUD display still slice-gated); no render/UI/index.html changes; no new deps; no OSTK artifacts; flagless default run verified inert by the pathcheck child.
