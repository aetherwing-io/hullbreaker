# T-009 — six-face lattice, dare pockets, hound stations, static-anatomy reveal

Frames from the DEFAULT run (`index.html`, no flags, FAR view) after the T-009
integration. Captured headless at 1440×900 through the same reflex policy the
`six-face-full-run` playtest script uses.

| shot | what to look at |
| --- | --- |
| `01-pocket-face1.png` | face 1's dare pocket: the deck steps down across the chasm, the mid lane runs over the landing, and the SPREAD capsule sits on the shelf tip out over the void (magenta, right of RIG). RIG is ~3.5% of screen height. |
| `02-corner-ritual-static-anatomy.png` | the corner ritual, mid-turn. The joint's ridge and buttress are the pivot; the next facet's deck and catwalks are ALREADY THERE on the right, receding into haze. Nothing assembles — this is decisions.md entry 3 as the shipped default. |
| `03-pocket-face2.png` | face 2's pocket approached from the deck line. |
| `04-hound-station-face2.png` | face 2's houndframe station (green, on the plate) standing on the pocket landing, with the pocket's mid lane directly above it — the answer to the charge is the route the shelf hangs from. |

## Honesty notes

- **Capture aid**: the capture script tops RIG's HP up every poll so one run can
  reach the later faces. Nothing else is modified — the wave gates still had to
  be shot down for the scroll to resume, and the geometry, spawns and camera are
  exactly what the shipped URL fields. The frames are evidence about
  *composition and placement*, not about difficulty.
- These are grey-box frames. Palette, glyph and readability work belong to the
  art/palette lane (T-010), not to this task.
- The floating slabs high in frame are the limb bake's `silhouette` pieces
  (distant anatomy), unchanged by this task.
