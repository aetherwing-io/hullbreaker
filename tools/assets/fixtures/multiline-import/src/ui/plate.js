/* FIXTURE — not part of the game. tools/assets/check.mjs must REJECT this file.
   A re-export binds the module exactly as hard as an import does, and this one
   also puts the specifier on its own line. */

export *
  from '../../assets/generated/textures/vent-plate.png';
