{
  "name": "polyp-vent-kill",
  "description": "ACCEPTANCE, target priority / the opening: the facetank funnel with fire held the whole run (?polyp=1 solo stage). The only scheduled input left is the fire hold itself (state-independent by design — the point is spraying through every armour state); all movement is closed-loop policy: the approach (bunny-hop off each actual landing while x<52.8, air-jump on each falling edge inside the chimney shaft x 38-47 below roof height, pinned-recovery scoped to x<57) climbs the chimney onto the roof and drops the bot onto the span, then the funnel parks it on post-mid inside the locked lane spraying level rifle shots at the bulb — CONFIG roots the bulb center exactly on the standing firing line, so aim is never the obstacle. While the iris is closed or dilating the shots visibly ping off the armour (a naive DPS race would kill a 6 hp target in under a second; here the kill cannot land before the first volley resolves), the first beam costs the bot one point, and the fire+vent opening that follows is where the kill actually happens — DESIGN's 'destroy it during an opening', measured. Once kills>=1 the x-gate releases and the bot runs out to the rejoin: expected (measured 3/3 across closed-loop tuning runs) VICTORY, kills 1, hp 2. With no timed movement inputs left there is no arrival-time fork for dispatch quantization to compound through; the old ~1-in-7 approach jam is gone with the timed hops that caused it.",
  "url": "index.html?slice=traversal&polyp=1",
  "viewport": { "width": 1280, "height": 800 },
  "durationMs": 13000,
  "moves": [
    { "hold": "fire", "fromMs": 100, "toMs": 12800 }
  ],
  "policy": {
    "rules": [
      { "when": "x<59.5", "do": { "hold": "right" } },
      { "when": "kills>=1", "do": { "hold": "right" } },

      { "when": "grounded && x<52.8", "do": { "tap": "jump", "holdMs": 380 } },
      { "when": "airborne && vy<0 && x>38 && x<47 && y<10", "do": { "tap": "jump", "holdMs": 380 } },
      { "when": "pinned && x<57", "do": { "tap": "jump", "holdMs": 380 } },

      { "when": "grounded && y>8 && x>57", "do": { "tap": "down", "holdMs": 260 } },
      { "when": "grounded && y>8 && x>57", "do": { "tap": "jump", "holdMs": 80 } },
      { "when": "grounded && y<3.5 && x>57 && kills<1", "do": { "tap": "jump", "holdMs": 380 } }
    ]
  }
}
