{
  "name": "polyp-facetank",
  "description": "ACCEPTANCE, cost: park a bot inside the lane the Iris Polyp locks (?polyp=1 solo stage) and measure what standing in the beam costs. Fully closed-loop — zero timed inputs: the approach is policy (bunny-hop off each actual landing while x<52.8, air-jump on each falling edge inside the chimney shaft x 38-47 below roof height, pinned-recovery scoped to x<57), then the funnel self-corrects whichever band the bot lands in: hold right only while x<59.5, down off post-high (down+jump drop-through) or up off the deck (jump onto the one-way catwalk) until the bot stands on post-mid inside the beam band, then stay. Expected rhythm (measured 3/3 across closed-loop tuning runs): tell -> fire costs exactly one point per iris cycle (i-frames outlast one volley, expire well inside the cycle), the beam's knockback shoves the bot out and the x-gate walks it back in — the faster policy approach fits THREE volleys in the window (hp 3->2->1, then the third empties the bar into the slice's HULL FALLBACK mercy: setbacks 1, hp restored), and the run classifies as 'stalled' because parking is the whole point. Reported as a damage count over ~3 cycles, not a pass/fail on completion — eating the beam is allowed to be a choice, it just has to cost. With no timed inputs left there is no arrival-time fork for dispatch quantization to compound through; the old ~1-in-7 approach jam is gone with the timed hops that caused it.",
  "url": "index.html?slice=traversal&polyp=1",
  "viewport": { "width": 1280, "height": 800 },
  "durationMs": 13000,
  "moves": [],
  "policy": {
    "rules": [
      { "when": "x<59.5", "do": { "hold": "right" } },

      { "when": "grounded && x<52.8", "do": { "tap": "jump", "holdMs": 380 } },
      { "when": "airborne && vy<0 && x>38 && x<47 && y<10", "do": { "tap": "jump", "holdMs": 380 } },
      { "when": "pinned && x<57", "do": { "tap": "jump", "holdMs": 380 } },

      { "when": "grounded && y>8 && x>57", "do": { "tap": "down", "holdMs": 260 } },
      { "when": "grounded && y>8 && x>57", "do": { "tap": "jump", "holdMs": 80 } },
      { "when": "grounded && y<3.5 && x>57", "do": { "tap": "jump", "holdMs": 380 } }
    ]
  }
}
