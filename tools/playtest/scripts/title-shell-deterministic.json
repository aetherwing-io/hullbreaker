{
  "name": "title-shell-deterministic",
  "description": "HISTORICAL I-018 title-shell regression probe. The current frame-scoped --deterministic mode deliberately rejects ?shell=title: MENU freezes the simulation clock and shell behavior belongs to the real browser input path. Run this script without --deterministic to test title start/fall-through; invoking it with --deterministic must fail validation before launching a server/browser.",
  "url": "index.html?slice=traversal&shell=title",
  "durationMs": 7000,
  "moves": [
    { "hold": "right", "fromMs": 600, "toMs": 6800 },
    { "tap": "jump", "atMs": 1400, "holdMs": 380 }
  ]
}
