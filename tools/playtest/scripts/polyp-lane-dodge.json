{
  "name": "polyp-lane-dodge",
  "description": "ACCEPTANCE, movement answer: bait the Iris Polyp's beam and dodge every volley (?polyp=1 solo stage). The early hops carry the bot over the chimney toward the span; from there movement is closed-loop: the funnel walks it into the locked post-mid lane only while the iris is quiet, commits any roof-tail crossing (never stalls in the band), and the moment the iris dilates (polypTell) while the bot stands in the lane it drops through the catwalk (down+jump, the game's own verb) below the band, re-mounting between volleys to re-bait. After 10.5s sim time it exits by vaulting the emplacement from the parked catwalk in a quiet window (a full jump from x<60 clears the bulb's contact circle) and runs out to the rejoin. Zero timed dodges: every polyp reaction keys off its telemetry state. EXPECTED (measured 5/5 across committed tuning runs): VICTORY, kills 0, every parked-cycle volley sweeping an EMPTY lane (bot on the deck below at fire time — the tell -> window -> answer loop). Honest variance: the first approach crossing and the release egress each sit on a dispatch-timing fork and may cost one contact/beam point (runs land at hp 3 or hp 1); a hit DURING a parked cycle, or kills>0, is the actual regression signal. KNOWN LIMIT (harness, not enemy): the open-loop approach hops fork on dispatch quantization and can jam a run in the chimney or dare pocket (~1 in 7 observed across all polyp scripts); the run then reports stalled/not-completed at x<57 with the polyp never engaged — re-run it.",
  "url": "index.html?slice=traversal&polyp=1",
  "viewport": { "width": 1280, "height": 800 },
  "durationMs": 15000,
  "moves": [
    { "tap": "jump", "atMs": 300, "holdMs": 380 },
    { "tap": "jump", "atMs": 1100, "holdMs": 380 },
    { "tap": "jump", "atMs": 1900, "holdMs": 380 },
    { "tap": "jump", "atMs": 2700, "holdMs": 380 },
    { "tap": "jump", "atMs": 3500, "holdMs": 380 },
    { "tap": "jump", "atMs": 4300, "holdMs": 380 }
  ],
  "policy": {
    "rules": [
      { "when": "x<52.8", "do": { "hold": "right" } },
      { "when": "y>5.8 && x<58.5", "do": { "hold": "right" } },
      { "when": "x<59.5 && y<5 && !polypTell && !polypFire", "do": { "hold": "right" } },
      { "when": "gameMs>10500", "do": { "hold": "right" } },

      { "when": "polypTell && grounded && y>5 && y<5.5 && gameMs<10500", "do": { "tap": "down", "holdMs": 260 } },
      { "when": "polypTell && grounded && y>5 && y<5.5 && gameMs<10500", "do": { "tap": "jump", "holdMs": 80 } },
      { "when": "grounded && y>8 && x>57 && !polypTell && !polypFire", "do": { "tap": "down", "holdMs": 260 } },
      { "when": "grounded && y>8 && x>57 && !polypTell && !polypFire", "do": { "tap": "jump", "holdMs": 80 } },
      { "when": "grounded && y<3.5 && x>57 && !polypTell && !polypFire", "do": { "tap": "jump", "holdMs": 380 } },

      { "when": "gameMs>10500 && grounded && y>5 && y<5.5 && !polypTell && !polypFire", "do": { "tap": "jump", "holdMs": 380 } }
    ]
  }
}
