{
  "name": "hullbreaker-deploy",
  "private": true,
  "version": "0.1.0",
  "description": "Dev-only itch.io deploy bundle builder + falsifying render test for HULLBREAKER (T-034/T-055). Not part of the game runtime.",
  "type": "module",
  "scripts": {
    "verify": "node verify-bundle.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.48.0"
  }
}
