# Archive

Superseded documents and sections land here, verbatim, so their history is
preserved without slowing down a new agent reading the live docs. `../decisions.md`
and `git log` are the authoritative trail for what changed and why — treat
anything here as retired reference, not current state.
